services:
  api:
    build: ./python-service
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./python-service:/app

  bot:
    build: ./bot
    depends_on:
      - api
    env_file: .env
    volumes:
      - ./bot:/app
